#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// Reverse a String
int main() {
char s[1000];
    // Read the string
    scanf("%s", s);
    // Find the length of the string
    int n = strlen(s);
    // Reverse the string by swapping characters
    for (int i = 0; i < n / 2; i++) {
    return 0;
}
